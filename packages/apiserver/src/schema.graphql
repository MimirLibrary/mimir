scalar DateTime

union BookUnionResult = Status | Error

type Error {
  message: String!
}
input WhereArgString {
  exact: String
  gt: String
  lt: String
  contains: String
  in: [String]
}

type Query {
  welcome: String!
}

type Mutation
