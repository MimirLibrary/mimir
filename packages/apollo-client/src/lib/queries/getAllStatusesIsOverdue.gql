query GetAllStatusesIsOverdue($locations: [Int!]) {
  getAllStatusesIsOverdue(locations: $locations) {
    id
    created_at
    returnDate
    material {
      id
      title
    }
    person {
      id
      username
      avatar
    }
  }
}
