query GetAllStatusesIsOverdue($location_id: String!) {
  getAllStatusesIsOverdue(location_id: $location_id) {
    id
    created_at
    material {
      id
      title
    }
    person {
      id
      username
    }
  }
}
