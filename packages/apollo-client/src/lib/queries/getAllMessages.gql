query GetAllMessages($locations: [Int!]) {
  getAllMessages(locations: $locations) {
    id
    created_at
    title
    message
    person_id
    person {
      id
      username
      avatar
    }
  }
}
