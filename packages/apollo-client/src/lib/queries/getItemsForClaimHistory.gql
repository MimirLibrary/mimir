query GetItemsForClaimHistory($person_id: Int!) {
  getItemsForClaimHistory(person_id: $person_id) {
    id
    status
    returnDate
    created_at
    material {
      id
      picture
      title
      author
      category
    }
  }
}
