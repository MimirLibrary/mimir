import { colors } from '../colors';

export const theme = {
  light: {
    colors: {
      primary_blue: colors.main_blue,
    },
    background: {
      primary_bg: colors.bg_primary,
    },
  },
  dark: {
    colors: {
      primary_blue: colors.main_blue,
    },
    background: {
      primary_bg: colors.bg_primary,
    },
  },
};

//The same colors, because there is no theme change in the design yet. Then сan add different colors.
