version: '3.9'
services:
  postgres:
    image: postgres
    env_file: .${NODE_ENV}.env
    volumes:
      - db:/var/lib/postgresql/data
    ports:
      - '5432:5432'

  apiserver:
    build:
      context: .
      dockerfile: apiserver.Dockerfile
    env_file: .${NODE_ENV}.env
    ports:
      - '3333:3333'
    depends_on:
      - postgres
    command: sh -c "
      npm run migration:run
      && npx nx run apiserver:build:production
      && node dist/packages/apiserver/main.js
      "

  frontapp:
    build:
      context: .
      dockerfile: frontapp.Dockerfile
    ports:
      - '80:80'

volumes:
  db:
