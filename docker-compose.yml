version: '3.9'
services:
  web:
    build: 
      context: .
      dockerfile: ./Dockerfile
  db:
    image: postgres
    env_file: .env
    ports:
    - ${POSTGRES_PORT}:${POSTGRES_PORT}
    volumes:
      - db:/var/lib/postgresql/data
volumes:
  db:

