version: '3.9'
services:
  postgres:
    image: postgres
    env_file: .env
    volumes:
      - db:/var/lib/postgresql/data
    ports:
      - '5432:5432'

  web-srv:
    image: nginx
    ports:
      - '80:80'
    volumes:
      - /srv/www:/usr/share/nginx/html:ro

volumes:
  db:
